{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "PYBIN=\"$CLAUDE_PROJECT_DIR/.venv/bin/python\"; [ -x \"$PYBIN\" ] || PYBIN=\"$(command -v python3 || command -v python || true)\"; [ -n \"$PYBIN\" ] || exit 0; \"$PYBIN\" \"$CLAUDE_PROJECT_DIR/tools/sdd_gate.py\""
          }
        ]
      }
    ]
  }
}
